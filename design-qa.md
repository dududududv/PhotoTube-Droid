# PhotoTube 设计 QA

## 2026-09-02 资产详情页系统相册查看器终版

本节是当前有效结论；下方深色画布、底部日期尺寸胶囊和取色背景方案均为历史记录，不再代表当前实现。

### 对照目标与归一化

- 视觉真值：Apple 官方 iOS 18 Photos 单图查看器，`/private/tmp/ios18-photos-viewer-reference.png`，原始 `490 × 1008px`；去除设备外框后的应用内容为 `/private/tmp/ios18-photos-viewer-content.png`，`444 × 960px`。
- 产品内风格基准：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-bff3d5e9-6699-4fb7-89be-ed78c50442af.png`；ColorOS 16 的双入口图库、浮动导航和信息上滑逻辑作为 Android 侧行为参考。
- 最终实现：`/private/tmp/phototube-viewer-v4-final.png`，Android 16 / API 36 原生 `1280 × 2856px`；等宽归一图为 `/private/tmp/phototube-viewer-v4-final-444.png`，`444 × 990px`。
- CSS 尺寸：不适用，原生 Jetpack Compose；实现设备逻辑视口约 `412 × 919dp`，系统密度约 `3.11`。参考与实现按 `444px` 内容宽度归一，保留 iPhone 与 Android 16 的纵横比差异。
- 对照状态：公开、可浏览、未收藏的静态照片；顶部与底部控件显示，胶片栏中当前照片被选中。

### 比较历史与修正

- P1：首页是浅色照片编辑体系，上一版却使用黑色画布、三个悬浮圆和日期尺寸胶囊，信息架构与系统相册查看器不一致。修正为浅色连续画布、居中的来源与时间、轻量返回/更多、真实胶片栏及固定核心动作。
- P2：旧版收藏放在顶栏，信息、编辑和删除藏在“更多”，高频操作不易发现。最终底栏按分享、收藏、信息、编辑、删除排序；标签、评分、归档、私密和回收站恢复等低频能力继续留在次级面板。
- P2：首轮手势实现被图片缩放手势消费。最终使用不抢占事件的 final-pass 观察器；实机左滑已从手机照片切到下一张兔子照片，上滑已打开真实照片信息面板。
- P2：首轮底栏使用偏厚的实心 Material 图标。最终换成 Outlined 图标，保留 `48dp` 触控区，视觉重量更接近系统图库。

### 最终五项检查

- 字体与排版：来源 `15sp / SemiBold`，日期 `12sp` 灰色；没有文件名重复、长路径常驻或标题抢占照片。
- 间距与节奏：顶栏保持一个轻量信息层；照片完整按比例显示；胶片栏 `58dp`、操作栏 `62dp`，两者合为一块连续底部表面，没有大胶囊、描边或卡片套卡片。
- 色彩与令牌：页面沿用首页的 `#F7F8FA`、白、黑和灰；底部使用 Android 16 空间毛玻璃与 `82%` 白色覆盖，不从照片取脏色。
- 图片与图标：原图、版本化真实缩略图与 Material Outlined 图标均为真实资源；没有占位照片、重复缩略图、手绘 SVG 或文字图标。
- 文案与内容：顶部显示真实来源目录和拍摄时间；照片信息面板继续展示服务端真实字段。iOS 的英文地点与工具胶囊没有机械复制，按 PhotoTube 数据和 Android 平台改写。

### 交互与可访问性

- 实机验证：点胶片缩略图、左右滑切图、上滑照片信息、单击显隐 chrome、信息按钮均可用；分享使用 Android Sharesheet，收藏、编辑和删除沿用原有真实 mutation。
- 所有底部动作与顶部图标均保留至少 `48dp` 触控范围；不可编辑照片会禁用编辑，不可读取原图时会禁用分享。
- 聚焦区域：归一化全图中的顶栏、胶片栏和操作栏在 `444px` 宽度下均清晰可读，因此未再制作会丢失上下文的局部裁切。
- 平台差异：参考图将部分动作组合为 iOS 圆角胶囊；实现有意采用 ColorOS/Android 更直接的五等分工具栏，并保留 Android 16 系统栏，不视为设计漂移。

### 结论

- P0：0
- P1：0
- P2：0
- P3：横向照片或更短屏设备仍建议补充一轮截图，但不阻断当前高屏 API 36 目标。

final result: passed

## 2026-09-02 资产详情页首页同体系复审（已被上方终版覆盖）

本节覆盖下方“资产详情页沉浸式改版”的旧结论；旧版的照片取色模糊背景和多层信息卡只保留为迭代记录。

### 对照证据

- 首页实机基准：`/private/tmp/phototube-restarted.png`，Android 16 原生 `1280 × 2856px`。
- 详情页控件态：`/private/tmp/phototube-viewer-static-final-v3.png`，Android 16 原生 `1280 × 2856px`。
- 详情页沉浸态：`/private/tmp/phototube-viewer-static-hidden-v2.png`，Android 16 原生 `1280 × 2856px`。
- 原始概念图：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-bff3d5e9-6699-4fb7-89be-ed78c50442af.png`。
- 改版前问题图：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-98311f3e-af86-4a5b-b598-ed8a53e6481b.png`。
- 验收状态：公开、可浏览的静态照片，尺寸 `1899 × 2532`；同一资产完成控件显示与点击隐藏两态验证。

### 本轮阻断问题与修正

- P1：照片取色模糊背景引入脏绿、棕色和厚重氛围，与首页的白、黑、浅灰编辑感不一致。改为稳定的 `#101114` 中性画布，不再从照片派生页面色彩。
- P1：详情页底部曾同时常驻文件名、路径、标签和两个动作，信息层级明显重于首页。主画布现在只显示日期与尺寸，其余信息全部收拢到“更多”。
- P2：首轮单层信息胶囊占满屏宽，仍像一张底卡。最终收至 `304 × 58dp`、圆角 `29dp`，与首页扁平双项胶囊保持相近比例和材质。
- P2：照片无法快速进入纯查看状态。静态照片现在可点击隐藏顶部和底部 chrome，再次点击恢复；视频点击仍由 Media3 播放控件处理。

### 最终检查

- 视觉：中性深色画布、乳白无描边玻璃、黑色图标和浅灰辅助文字与首页令牌一致；没有彩色渐变、硬边框或叠套卡片。
- 几何：顶部按钮 `48dp`、间距 `10dp`；底部胶囊 `304 × 58dp`、圆角 `29dp`，没有额外边框与内部按钮。
- 层级：照片始终是唯一大面积内容，日期与尺寸只承担定位信息，文件名、路径和标签不再竞争视线。
- 交互：返回、收藏、更多、1–3 倍缩放、静态照片 chrome 显隐、标签、照片信息、动态照片与视频播放路径保留。
- 平台：仅面向 Android 16 / API 36，系统栏、边到边和真实空间毛玻璃均按该版本验收，不提供旧系统视觉降级。

### 独立复审

- reviewer 最终评分：`9.0 / 10`。
- 阻断问题：`P1 = 0`、`P2 = 0`；无需继续修改的 `P3 = 0`。
- 结论：`304 × 58dp` 胶囊已与首页底部导航的扁平比例同源，尺寸文字对比度清晰；详情页保留首页的单色体系、无描边玻璃材质与圆形工具按钮，并以中性黑色沉浸画布区分浏览和查看场景。

final result: passed

## 2026-09-02 资产详情页沉浸式改版（已被上方复审结论覆盖）

### 对照证据

- 问题截图：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-a788083e-1f5b-46bd-af27-93a4f35666c4.png`，原图 `832 × 1676px`；裁出应用屏幕后归一到 `1280 × 2856px`。
- 设计语言基准：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-bff3d5e9-6699-4fb7-89be-ed78c50442af.png`，沿用无描边椭圆、低对比毛玻璃和照片优先层级。
- 最终实现截图：`/private/tmp/phototube-viewer-redesign-final.png`，Android 16 模拟器原生 `1280 × 2856px`。
- 同画布全屏对照：`/private/tmp/phototube-viewer-before-after.png`。
- 聚焦交互证据：`/private/tmp/phototube-viewer-info-final.png` 与 `/private/tmp/phototube-viewer-tags-final.png`；信息和标签面板均由最终详情页真实点击打开，因此不再额外制作静态局部裁切。
- 状态：公开、可浏览的横向照片，未收藏，六个标签，原图尺寸 `5800 × 3871`。

### 首轮发现与修正

- P1：白色工具栏、纯黑画布和白色信息区把页面切成三段，照片没有成为视觉主体。修正为全屏媒体画布，使用同一张照片的认证缩略图生成裁切、模糊并压暗的背景，原图保持 `ContentScale.Fit` 和原比例。
- P1：文件名在顶部和底部重复，长标签与“照片信息”挤在同一文本行并溢出。顶部移除文件名；底部只保留日期、单行文件名、单行相对路径、两项标签预览与 `+N`，照片信息成为独立椭圆动作。
- P2：操作栏占据固定白色区域。返回、收藏和更多操作改为三个 `48dp` 无描边 Android 16 毛玻璃圆，直接悬浮在媒体色相上。
- P1：首轮运行截图的状态栏使用白色背景和白色图标。最终将详情页状态栏切为深色沉浸样式，并在离页时恢复全局浅色系统栏。

### 最终检查

- 字体：日期为 `18sp` 半粗体，文件名、路径、尺寸和标签使用 `12–13sp` 次级层级；无重复标题、错误换行或截断溢出。
- 间距：顶栏左右 `14dp`，操作圆间隔 `10dp`；底部玻璃卡左右 `14dp`、圆角 `28dp`，信息区与系统导航区留有安全距离。
- 色彩：照片原色是主视觉；背景只保留同图的低频色相，顶部状态栏为深绿黑，玻璃表面使用乳白雾化且没有描边。
- 图片：原图没有裁切或拉伸，背景来自同一资产而非占位图；缩放、MD 占位到原图过渡和内容寻址 URL 均保留。
- 文案：底部只出现一份文件名；标签折叠为两项加数量，“照片信息”与“管理标签”均可正常打开。
- 交互：返回、收藏、更多操作、1–3 倍缩放、标签、照片信息、动态照片与视频播放路径保持原功能；信息和标签两个主要入口已在 API 36 实机态点击验证。

final result: passed

## 2026-09-01 双项主导航终版

本节是当前有效结论；下方三项菜单与两侧圆形按钮的记录只保留为迭代历史，不再代表当前产品结构。

- 胶囊视觉参考：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-23fe0665-edf1-42d6-bd82-36a48b239b84.jpg`
- “所有照片”激活截图：`/private/tmp/phototube-two-tab-dock-final.png`
- “相册”激活截图：`/private/tmp/phototube-two-tab-swipe-final.png`
- 胶囊同画布对照：`/private/tmp/phototube-two-tab-reference-comparison-final.jpg`
- 账号管理截图：`/private/tmp/phototube-management-hub.png`
- 任务中心截图：`/private/tmp/phototube-task-center-route.png`

最终导航只保留“所有照片 / 相册”两个稳定一级目的地。胶囊可见尺寸为 `224 × 52dp`，内部选中椭圆为 `104 × 44dp`，整体触控高度为 `56dp`；乳白玻璃底层在纯白页面上仍有可辨认的灰雾层级，但没有描边、外套框或左右独立圆形按钮。点击任一分段与在胶囊上水平拖动都能切换真实页面，松手后由弹簧动画吸附到对应分段。

扫描、导入、队列、系统状态、缓存、归档、私密空间、回收站、重复项、XMP、回忆屏蔽与标签管理均不再占用底部导航。用户点击头像进入“账号与管理”，其中“后台与系统”分组提供“任务中心”和“系统状态与缓存”；任务中心已用真实路径相册同步队列验证。非破坏性编辑继续从照片查看器进入，不设置“创作”一级入口。

验证环境为 `PhotoTube_API_36`，物理视口 `1280 × 2856px`，Android 16 / API 36。真实点击、水平滑动、头像进入管理页、任务中心进入与返回均已运行验证；`224` 条 Debug JVM 单元测试与 Debug APK 构建全部通过。

final result: passed

## 2026-09-01 原始概念图回归校准

本节是当前有效结论；下方多轮“继续压平”记录仅保留为修改历史，不再代表最终尺寸。

- 原始概念图：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-bff3d5e9-6699-4fb7-89be-ed78c50442af.png`
- Android 16 时间轴终版截图：`/private/tmp/phototube-timeline-final.png`
- Android 16 路径相册终版截图：`/private/tmp/phototube-path-detail-final.png`
- 同画布视觉对照：`/private/tmp/phototube-reference-comparison-final.jpg`

本轮针对用户指出的整体偏离重新以原始概念图为唯一布局基准，修正了四类问题：

1. 时间轴标题由过大的 `44sp` 收至 `36sp`，标题、月份、时间选择器改成同一视觉块，减少无意义纵向空白。
2. 年/月/日选择器由 `196dp` 收至 `144dp`；可见轨道只有 `40dp` 高，但每段仍保留 `48 × 48dp` 触控区。
3. 编辑式拼贴回到 `2 + 3 + 2` 的连续节奏，行高调整为 `142dp / 144dp / 128dp`，照片间仅保留 `4dp`。
4. 路径相册由“管理表单优先”改成“照片优先”：扫描管理和历史默认折叠，照片行距从 `18dp` 恢复为 `4dp`，常驻的照片菜单圆全部移除，操作改为长按出现。

底栏保留用户后续明确要求的“照片 / 图集 / 创作”文字滑块，而不退回概念图中的三图标样式；可见高度从失衡的 `24dp` 恢复为 `36dp`，仍保持无边框、椭圆选中态和 Android 16 毛玻璃。

验证环境为 `PhotoTube_API_36`，物理视口 `1280 × 2856px`，API 36。最终构建已通过 Kotlin 编译与全部 Debug JVM 单元测试，并在该模拟器安装、启动和截图复核。

final result: Android 16 runtime verified; reference comparison completed

## 对照信息

- 最新视觉基准：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-23fe0665-edf1-42d6-bd82-36a48b239b84.jpg`
- 最终实现截图：`/private/tmp/phototube-dock-24dp-api36.png`
- “图集”激活截图：`/private/tmp/phototube-segmented-dock-albums.png`
- 参考图聚焦裁切：`/private/tmp/reference-dock-crop-v2.jpg`
- 实现聚焦裁切：`/private/tmp/implementation-dock-flatter-crop.png`
- 同画布对照：`/private/tmp/dock-reference-comparison-flatter.png`
- 模拟器视口：`1280×2856px`，Android 16 / API 36 / 16KB 页大小。
- 视觉基准尺寸：`1080×2354px`。

## 目标结构

参考图的品质来自清楚的三段分区，而不是多层边框：

1. 左侧是独立的圆形布局按钮。
2. 中间是无边框长胶囊，内部为“照片 / 图集 / 创作”三段文字导航。
3. 右侧是独立的圆形显示选项按钮。
4. 选中态是横向白色椭圆，可点击，也可随着手指左右拖动。

## 最终检查

### 几何与节奏

- 中段可见区域为 `242×24dp`，两侧按钮保留 `48dp` 触控热区、可见圆为 `36dp`，分组间距为 `9dp`。
- 中段宽度约占模拟器内容宽度 `54%`，参考图约为 `56%`；两侧按钮占比均约 `13%`。
- 三个区域完全分离，没有外框、嵌套白边或彼此粘连。
- 选中椭圆占据一个完整分段，高度为 `22dp`，左右移动后几何不变。

### 颜色与材质

- 三个表面均采样其背后的照片并执行真实空间模糊，不是普通透明填充。
- 玻璃保留背景色相，但人物、文字和建筑细节被雾化；导航文字保持锐利。
- 选中项使用高不透明乳白渐变和极轻阴影，没有描边。
- 所有阴影均为低对比软阴影，不出现灰色矩形或硬轮廓。

### 字体与图标

- 可见文字与参考图一致，为“照片 / 图集 / 创作”。
- 三段文字使用同一字号；选中项仅提高字重，不改变字号或基线。
- 左右动作使用 Compose Material 官方图标，没有字符、手绘 SVG 或截图组件。

### 交互

- 点击“照片”与“图集”可进入对应页面，页面激活项保持正确。
- 在中间胶囊上水平拖动可跨段切换，松手后用弹簧动画吸附。
- 左右圆形动作区均达到 `48dp` 以上触控尺寸。
- 点击反馈受圆形或椭圆裁切，不会出现方形波纹。

## 同画布结论

把参考图和最终模拟器裁切归一到同一画布后，中心胶囊宽高比、两侧按钮比例、三段文字基线和选中椭圆占比均处于同一视觉区间。实现保留 Android 16 原生系统栏和项目已有照片内容，因此不复刻参考图的系统导航条与相册缩略图，但底栏本身的层级、形状和分区关系已匹配。

## 本轮修正记录

1. 删除旧的三图标胶囊与黑色新增按钮。
2. 重构为“两侧独立动作 + 中间文字分段”的三组结构。
3. 移除全部边框与外壳留白，仅保留自然圆角、模糊和软阴影。
4. 将选中态改为占据单个分段的白色横向椭圆。
5. 修正页面语义：“照片”对应时间线，“图集”对应合集页面。
6. 在 Android 16 模拟器验证点击与水平拖动，两种激活状态均截图复核。
7. 根据“再压平一点”的反馈，把中段高度由 `64dp` 收至 `56dp`、动作按钮由 `58dp` 收至 `52dp`，同步缩小内部椭圆、图标和字号；横向比例保持不变。
8. 第二轮继续把中段收至 `50dp`、动作按钮收至 `50dp`，滑块内边距改为 `3dp`，并降低阴影高度；触控区域仍高于 `48dp` 下限。
9. 第三轮把中段和动作按钮统一收至 `48dp`，滑块收至 `40dp`，底栏垂直留白由 `12dp` 收至 `10dp`；主轨阴影由 `7dp` 降至 `4dp`，滑块阴影由 `3dp` 降至 `1.5dp`，形成更扁、更贴近照片面的玻璃层。
10. 第四轮把中段可见高度收至 `44dp`、滑块收至 `38dp`，动作按钮可见圆同步收至 `44dp`，阴影降至 `3dp / 1dp`；左右按钮仍使用 `48dp` 透明触控热区，兼顾扁平比例与可点性。
11. 第五轮按“再压平一点”的反馈，将中段可见高度和两侧可见圆统一收至 `40dp`，内部椭圆收至 `34dp`；底栏垂直留白降至 `6dp`、主阴影降至 `2dp`，文字与图标同步微缩，透明触控热区继续保持 `48dp`。
12. 第六轮继续将中段可见高度压至 `36dp`、内部椭圆压至 `32dp`，两侧可见圆保持 `40dp`，形成“中间更扁、两侧略高”的参考图比例；主轨和滑块阴影降至 `1dp / 0.5dp`，白色覆层同步减弱。中段新增独立 `48dp` 透明触控外壳，视觉变薄但点击与拖动热区不缩水。
13. 第七轮把中段进一步压至 `32dp`、内部椭圆压至 `28dp`，两侧可见圆同步收至 `36dp`；文字降至 `12sp`、动作图标降至 `18dp`，但三段与两侧按钮的透明触控热区仍保持 `48dp`。
14. 第八轮只继续压平中间导航：可见轨道由 `32dp` 收至 `28dp`、内部椭圆由 `28dp` 收至 `24dp`，主轨阴影降至 `0.5dp`；横向宽度、三段间距和 `48dp` 点击/拖动热区保持不变，避免“变扁”同时损失操作性。
15. 第九轮继续把中段轨道由 `28dp` 压至 `24dp`、内部椭圆由 `24dp` 压至 `22dp`；两侧可见圆、横向宽度、分组间距和 `48dp` 交互热区保持不变。最新 APK 已在 Android 16 / API 36 / 16KB 页大小模拟器截取真实照片背景画面，并通过 `224` 条 JVM 单测、Debug APK、AndroidTest APK 与 Lint（`0` error / `25` warning）完整门禁。

## 遗留差异

- P3：参考图左侧是品牌定制的“卡片 + 三点”图标；当前实现使用官方 `GridView`，语义一致但造型不同。为避免低质量手绘资产，本轮保留官方图标。
- 第九轮已有 API 36 最新 APK 截图；当前仍未用同一素材与参考图生成像素叠图，因此只能证明运行时几何、无边框、触控区和真实毛玻璃路径生效，不能把该截图扩大描述为最终像素级一致。

final result: runtime verified; pixel overlay pending

---

# 远端照片时间线设计 QA（2026-09-01）

## 对照信息

- 视觉基准：`/var/folders/sq/grfl_r3132gdgbm6mylf_n580000gn/T/codex-clipboard-eeac9abc-4119-4e32-9381-d7a2f4b8677c.png`。
- 基准内容区：日标题、`2 + 3 + 2` 编辑式拼贴、下一日三列预览、年/月/日椭圆选择器。
- 代码实现：`RemotePhotoTimelineScreen.kt` 与 `TimelineGrouping.kt`。
- 目标 Android 版本：API 36，不实现旧系统降级样式。

## 已实现几何

- 主内容保持 `24dp` 左右基线。
- 图片间隙统一 `4dp`，圆角统一 `12dp`。
- 七张组首三行高度为 `150dp / 152dp / 136dp`。
- 首行两图宽度比为 `1.72 : 1`，第二行三等分，第三行两等分。
- 三张日组使用一行三等分，对应基准中第二日的预览节奏。
- 时间选择器为 `196 × 52dp`，内边距 `3dp`，选中项是没有描边的白色椭圆滑块。
- 日期标题为 `24sp` 粗体，今天/昨天作为次级强调；后端未提供地名时不硬编码“上海”。

## 功能验证

- `TimelineGroupingTest` 验证年/月/日标题、今天/昨天、offset 日界与 `2 + 3 + 2` 节奏。
- 选择器切换会请求对应 `YEAR/MONTH/DAY` 摘要，取消旧请求并把列表回到顶部。
- 拼贴单元仍通过 `LazyPagingItems[index]` 读取，未破坏追加页预取。

## 设备验证状态

- 已在 `PhotoTube_API_36`（Android 16 / API 36 / 16KB 页大小）安装最新 APK，并用真实 10 项路径相册数据验证时间线、认证缩略图与底栏运行时渲染。
- 最新截图为 `/private/tmp/phototube-dock-24dp-api36.png`；当前数据顺序与参考稿不同，因此尚未生成同素材像素叠图。
- 几何与功能门已解除，像素级一致仍需同素材、同视口的归一化对照后单独下结论。

final result: runtime verified; pixel overlay pending
